% colocar os valores da matriz entre os ;, neste momento é uma matriz x por 3
% os valores entre ; são separados por espaço 
% os valores decimais são separados por .
matriz = [ ; ;];

% descobrir o determinante da matriz
determinante = det(matriz);

% descobrir a inversa da matriz
inversa = inv(matriz);

